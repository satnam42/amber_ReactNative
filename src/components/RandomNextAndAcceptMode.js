import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RandomNextAndAcceptMode = () => {
  return (
    <View
      style={{
        backgroundColor: 'blue',
        marginBottom: 70,
        flex: 1,
      }}>
      {/* header */}
      <View style={{width: '100%', backgroundColor: 'red', height: 30}}></View>

      {/* body */}
    </View>
  );
};

export default RandomNextAndAcceptMode;

const styles = StyleSheet.create({});
